[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-cicd"
version = "0.0.0"
description = "Scaffolded agentic CICD pipeline package"
authors = [{name = ""}]
